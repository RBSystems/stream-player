import { StreamContainer } from "./stream-container.model";

describe("StreamContainer.Model", () => {
  it("should create an instance", () => {
    expect(new StreamContainer()).toBeTruthy();
  });
});
